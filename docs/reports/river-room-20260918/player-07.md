# 玩家 7 独立观察报告（最终）

- 身份：A7长昵称测试十九字符玩家，实际座位索引 6（界面 7 号位），1280×800。
- 房间：https://rr.zandz.nexus/r/d3_IWZg4tJEq。
- 本 Agent 从第 14 手接管独立观察；之前手牌来自同一真实玩家身份的房间历史，不能声称此前各手 UI 由我实时观看。
- 动作执行边界：独立写入 `p7-policy.json`（raiseOnce=18），下注/投票由共享控制器协调执行；我真实点击了本人座位和弹窗关闭按钮，未与控制器争抢下注。
- 为降低本机浏览器负载，后续 P1–6 改用同身份 WebSocket + API 参与，P7–9 保留浏览器渲染；轻量连接曾未自动重连，后续控制器补充重连并使用 HTTP 快照收尾。这是测试执行方式变化，没有修改线上产品，也不代表全程九个页面持续在线。

## 已亲眼查看的 UI

1. 第 14 手 1280×800 桌面，长昵称在座位上省略显示，底部行动栏全文可见；座位卡/手牌/徽章未见文字换行或互相遮挡。大额下注 9,002、总底池 9,857 均完整。
   - 截图：`../main-run/screenshots/p7-initial-longname.png`。
2. 本人信息弹窗标题完整显示长昵称；筹码 3,453、净输赢 +1,955、赢池 1 次、被清台 2 次清晰，无溢出。成就规则在弹窗中有解释。
   - 截图：`../main-run/screenshots/p7-achievement-details.png`。
3. 第 15 手亲眼查看五位筹码 13,789、底池 803 和全下标记，无明显金额截断。截图：`../main-run/screenshots/p7-h15-allin.png`。
4. 弹窗截图背景一度出现重连提示；第 15 手截图亦见部分其他席位离线标记。测试机同时跑多页面且正在调整负载，主 Agent 确认存在测试控制器连接问题，不据此认定产品缺陷。

## 成就归属核对

第 14 手开始时历史数据：第 6 手本人在发两次牌中赢第 1 组主池及部分边池（won=1,713、delta=+1,213）；第 7 手仅赢边池（won=5,514）；第 8 手仅赢边池（won=472、delta=-2,014）；第 13 手其余玩家弃牌赢池 +3。页面显示赢池 1 次，与页面说明“独赢主池；发两次须独赢两组；平分/只赢边池不计”一致。没有把获得边池或只赢单组误认作成就错误。

第 14 手后自己被清台累计变为 3，与该手归零一致；完整结算数学复核由主 Agent 独立核算，不将阅读数据等同于已重算牌力。

## 当前问题

暂未发现本观察范围内可独立确认的新产品缺陷。主 Agent 已登记的探索房 Missing main pot 卡死以及结算展示窗口问题不重复验证或冒称为本 Agent 首次发现。

## 最终结果与证据边界

用户后续将目标改为 20 手。主 Agent 最终核实：主房完成 18 手，每手均有 9 个玩家身份获牌；第 19 手仅 6 人获牌，部分身份长时间断线后进入 AWAY，且再次触发 `Missing main pot` 导致无法完成。目标 20 手未达到；另一个探索房的 6 手不能并入本房充数。

- 主房卡死证据：`../main-run/hand-019-blocked.json`、`../main-run/server-final-errors.log`。
- 主 Agent 独立运行 `verify.py`，已完成的 18 手派彩复算通过。这是主 Agent 的核实结果，不是本 Agent 逐手独立重算。
- 主 Agent 调用房主结束接口收到 HTTP 200，但最终 `closing=true`、`closed_at=null`，房间未正常关闭；清理证据：`../main-run/cleanup-status.json`。浏览器和控制器已关闭。
- 本 Agent 的直接 UI 观察集中于第 14–15 手；历史成就复核读取当时房间数据并与线上弹窗说明核对。第 16–19 手终态由主 Agent 提供，本 Agent 没有额外线上复验。
- 控制策略文件已写入，但不能仅凭此断言第 18 手实际执行了加注 18；动作事实以共享控制器日志为准。

本人的长昵称、五位筹码、成就弹窗范围未发现新的可确认布局缺陷。全下暂时归零与结算后被清台的成就计数在所观察数据中一致。不能据此宣称所有牌局功能或所有结算分支均已验收。

未覆盖无限昵称、六位以上筹码、超长小数/负数输入、辅助技术与所有浏览器组合；不宣称最后几手新增单人否决覆盖。

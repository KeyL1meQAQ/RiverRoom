# 玩家 4 独立观察报告（320×740 窄屏）

- 线上房间：`https://rr.zandz.nexus/r/d3_IWZg4tJEq`，昵称 `A4窄屏320`，第 4 号座位。
- 观察方式：独立子 Agent 阅读该玩家真实浏览器截图与 DOM、服务端状态；下注与投票由同一在线控制器按覆盖策略执行。不是由 Agent 逐次人工决定所有动作。
- 本 Agent 写入策略 `main-run/players/p4-policy.json`：`{"raiseOnce":12}`；本人实际进行页面滚动与快照检查，未修改产品代码。
- 独立重点：窄屏单/双公共牌、多个边池、本人底牌和成牌、筹码及获胜信息、窗口到期与滚动可达性。
- 完成范围：本 Agent 介入时主房约第 6 手，持续定向观察至第 12 手完成；核对留存的第 1–12 手该玩家视角结果。结束时线上控制器已进入第 13 手，玩家继续留在同一房间参与后续测试。

## 已确认问题 P4-01 / P2：亮牌窗口结束过早清空本手结算说明

**现象：** 房间暂停、本手尚未被下一手替换时，5 秒自愿亮牌窗口到期后，玩家框的绿色获胜金额、公开牌型以及赢家框高亮一起消失。公共牌与底牌的获胜组合高亮仍保留。用户看到的是“本手底池 6,500”和高亮牌，却不能直接从玩家框核对谁收了多少；双公共牌和多边池时尤其难理解。

**真实证据：**

- `main-run/screenshots/h006-result-p4.png`：第 6 手双公共牌结算，获胜金额、牌型和框高亮已经消失。
- `main-run/screenshots/p4-h6-scroll.png`：本 Agent 实际滚动到底部后的同一手截图，仍无结算说明，排除了信息只是被固定操作区挡住的情况。
- `main-run/states/h006-result.json` 第 4 个视角：`number=6`、`phase=between`、`paused=true`，`server_time=1789696496.5962856`，`hand.reveal_until=1789696496.3778481`。公开牌型与获胜结果仍在服务端数据中：A7 收 1,713，A8 收 2,349，A1 收 900，A6 收 1,538。
- 对照 `main-run/screenshots/h005-result-p4.png`：窗口内，双行牌型与绿色金额均显示。
- 独立代码核对：`src/main.tsx:577` 的 `showingResult` 与 `reveal_until` 绑定；第 693、697–700 行分别用该条件隐藏公开牌型、获胜金额及赢家框。

**依据与边界：** `docs/acceptance.md:117` 约定进入下一手后获胜金额消失，`docs/requirements.md:214` 要求保留赢家框高亮。牌型在暂停期间保留的具体时长没有同等明确的单条约定，其消失另列为体验影响。5 秒之后禁止继续主动亮牌本身正确，问题在于把已公开结果的展示生命周期也绑定到了操作窗口。

**建议：** 已公开结果、获胜金额、赢家框延续到下一手替换或牌面清除；自愿亮牌按钮单独遵守 5 秒期限。优先回归暂停、补码等待、双公共牌、多边池，以及刷新后的结果一致性。

## 本人检查通过的项目

- **复杂桌面可纵向访问：** 第 6 手实时 DOM 中 `.room-main` 宽度 320，`scrollWidth=clientWidth=320`；可见高度 544、内容高度 664。滚动到 `scrollTop=120` 后，本人框、底牌完整进入可视区域。默认视图看不到本人框，不能据此认定为遮挡缺陷；项目明确允许复杂状态滚动。
- **双公共牌：** 第 5、6 手两组牌按 1/2 上下显示，同时可读。第 6 手投票截图显示主池和边池 1–7；两个投票按钮可读且留在底部操作区。
- **自己的成牌：** 第 5 手本人 Ad/Jd 对第一组为一对 A、第二组为一对 K；第 6 手本人 9c/4c 对第一组为一对 3、第二组为高牌 A，与状态标签一致。第 10 手本人 9s/2s 的高牌 A 显示正确。
- **普通结算：** 第 10 手截图在滚动后完整显示本人底牌与成牌；A5 的 `+126` 数额完整，没有在数字中间换行，已公开的其他玩家成牌可读。
- **第 12 手：** 本人日志记录跟注 15、三轮过牌；截图本人 7d/Kd 对 Qs/3s/Js/5c/Kc 显示一对 K，符合牌面。桌心 144、牌型、D 按钮、本人徽章未互相遮挡。
- **结果数值抽查：** 第 1–12 手，逐手 `sum(result.delta)=0`，`sum(awards.amounts)=sum(result.won)`；见 `main-run/players/p4-ledger-sample.json`。这只确认结果内部守恒，未独立证明所有牌力判断和边池资格正确。
- **徽章：** 已查看的第 5、6、8、10 手截图中，右上角徽章未遮挡本人点数、花色、筹码或牌型；未将图标尺寸较小主观认定为功能缺陷。

## 覆盖边界

- 本 Agent 没有独立人工穷举全部边池、奇数筹码与牌力组合；服务端获胜金额一致性和总额守恒只能排除部分错误，不能替代整套规则审计。
- 早期探索房第 7 手死锁由主 Agent 确认和取证，本报告不把它写成本人现场复现。
- 未测重连、离线重入、真实手机触控、浏览器缩放、辅助功能及超大金额边界。
- 本人截图、DOM 和日志观察是定向采样，不宣称逐帧观看全部动作。

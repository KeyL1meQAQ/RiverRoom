# 玩家 3 独立报告（360×740）

## 身份、方法与覆盖边界

- 初轮线上房间 `dh9xXfs7_-P1`：A3手机360，ID `G2kIQQzrCTTx`，第 1–5 手实际入局；第 6 手因测试器未及时审批重买入成为观战者，第 7 手亲自重新申请入座 300，并由玩家 1 Agent 批准。初轮 6/7 手不能算九人入局手。
- 主验收线上房间 `d3_IWZg4tJEq`：同名玩家，ID `PFD9qpX35aAp`，观察前三手，后续由控制器保持该身份继续游戏。
- 本人编写 `p3-policy.json` 指定普通手满足条件时加注到 10；特殊手服从共同覆盖计划。实际下注、投票由控制器通过线上 API 执行。本 Agent 独立检查该会话的真实网页截图、DOM 和状态，并亲自发出一次重新入座申请。不能表述为全程手动点击下注按钮。
- 初轮详细材料见 `../../players/p3-report.md`；本文为最终结论。未修改产品代码。

## 已确认问题

### P1：成就统计异常阻断结算，超时处理反复失败，牌局卡死

初轮第 7 手，服务端最新状态仍为 `betting`、`seq=2`，行动人为 A5。通过服务器 fresh 接口独立核查：完整行动时钟截止 `1789695945.103552`，而 `server_time=1789696102.1005595`，超时 **156.997 秒**仍未自动推进。这不是只读到了旧 WebSocket 缓存。

主 Agent 随后取得服务日志，本人交叉读取确认同一房间反复 `Room tick failed for dh9xXfs7_-P1`；堆栈落在 `backend/achievements.py:33 main_winner`，错误为 `ValueError: Missing main pot`。本地 `backend/game.py` 的超时分支也显示暂停后续发牌不应阻断本手超时。

影响：无法通过正常倒计时结束本手，后续牌局无法开始。建议修复成就统计对该结算结构的兼容性，并保证统计失败不会回滚已完成的核心结算；具体触发条件由主报告归纳。

证据：初轮 `players/p3-h7-expired-clock-fresh.json`（已删除 recovery_code）、`server-errors.log`。这是本 Agent 提交的已确认严重问题。

## 已复核，不列为缺陷

### 结算初始视口看不到本人座位，但可以滚动完整查看

初轮第 2/4/5 手及主验收第 1/2 手结算时，默认滚动位置下本人座位位于固定操作栏下方。首次只看截图容易误认为被永久遮住。新增滚动接口后，本人实际验证主验收第 1 手：

- `.room-main` 内容高 664、可见高 544，滚动位置由 0 到 120。
- 本人座位 y 从 649 到 529，底部约 603，固定操作栏顶为 616。
- 滚动后自己的底牌、姓名、296 筹码和“一对[4]”全部可见；亮牌操作始终可见。

因此按 `docs/acceptance.md` 允许复杂结算纵向滚动的约定，**不列为信息不可访问缺陷**。可考虑增加滚动提示或在结算时优先保留本人座位，属于体验优化。

证据：`screenshots/p3-h1-between.png`、`screenshots/p3-h1-between-scrolled.png`、`players/p3-h1-between-inspect-before.json`、`players/p3-h1-between-inspect-after.json`。

## 通过的观察

- 普通下注时 360×740 可看清本人底牌、姓名、筹码和行动区，未发现横向溢出；长昵称省略显示，没有挤压相邻座位。
- 初轮第 2 手翻牌前，本人状态仅含 `Jd/Ts`，其他 8 家无底牌数据且截图均为牌背。初轮第 1 手结算后也没有泄漏全部未公开底牌。主验收第 1 手本人状态仅含 `Qh/5c`。
- 初轮第 4/5 手本人分别以 328/300 剩余筹码跟注全下，合法行动 `can_raise=false`，请求均返回 200。第 4 手截图显示本人 0 筹码与“全下”文字；投票前没有提前显示重买入。
- 初轮第 4 手发两次投票按钮、主池和 7 个边池金额可见；本人拒绝后发一次。第 5 手同意发两次，两个公共牌组同时显示，摊牌玩家牌型按 ①/②区分。
- 初轮第 1 手本人 Q♦3♣，公共牌 Q♠3♦K♣9♠5♦，两对 Q/3 收到 36；300 − 4 + 36 = 332，净赚 32，与状态一致。
- 所复核手的收支守恒检查没有发现差异：净变动合计为 0、持有筹码合计等于净买入、获奖合计等于实际分配合计。此项不代替所有牌力及边池资格的独立重算。

## 不应混入产品缺陷的测试器问题

初轮暂停后错误等待 `phase=waiting`、驱动读取稍旧 WebSocket 状态、补码审批没有赶上截止时间，均是测试流程问题。测试驱动停止期间出现的普通超时弃牌不应判为卡顿；与有 fresh 状态和服务异常日志支持的“第 7 手卡死”应明确区分。

补充主验收第 3 手：9 人入局，其余玩家弃牌后 A5 赢得对手留下的 1 筹码，`revealed=[]`；玩家 3 截图没有看到赢家底牌。本人净变动 0，手级净变动合计 0、持有筹码与净买入均 3600、获奖与分配均 1。证据 `hand-003.json` 与 `screenshots/h003-result-p3.png`。本报告观察范围最终为探索房前 5 手玩家参与及后续卡死诊断、主验收房前 3 手。

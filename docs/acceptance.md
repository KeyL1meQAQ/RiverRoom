# 验收场景

此文件定义验收目标，已执行的测试及实际结果见 verification.md。Straddle 现场选择已确认为 5 秒，不消耗 TimeBank；拒绝、超时或离线视为不执行，筹码不足时跳过询问。

## 房间与身份

- 无登录状态下创建房间，创建者先观战；分享链接在另一浏览器打开后先观战，选空位并申请买入。
- 房主批准申请后入座，下一手开始参与；同座位竞争最多一人获批，不重复记买入。
- 刷新及断线重连保留身份、座位、底牌、筹码和 TimeBank，不重置正在进行的行动时间。
- 跨设备输入召回码后接管同一身份，旧设备操作被拒绝，旧召回码失效；房主身份也随参与者恢复。
- 离座后同浏览器再次申请入座保留累计账目及 TimeBank，不产生新的参与者身份。
- 他人及观战者不能收到未公开底牌、完整牌序、召回码或身份凭据。

## 牌局与计时

- 使用固定牌序验证普通下注、全下、短码加注与重新开放加注权、多重边池及平分，结算前后筹码守恒。
- 两人局按钮兼小盲，翻牌前先行动、翻牌后后行动；多人离座或 AWAY 时按空按钮规则轮转。
- 房间开启 Straddle 不自动扣筹码；本手 UTG 在发底牌前明确同意才执行 2 倍大盲，且翻牌前行动顺序和加注基准正确。
- 发两次牌询问所有未弃牌玩家；只要一人拒绝或 15 秒未响应就只发一次，全部明确同意才发两次；已有公共牌保留，余下牌不重复。
- 每个主池与边池独立拆分，两次发牌及多人并列时按已确认规则分配奇数筹码，日志可核对。
- 普通摊牌从河牌最后下注或加注者开始；河牌全员过牌则从按钮左侧第一位未弃牌者开始，翻牌或转牌的下注者不影响河牌全过的顺序。后台计算至最后一位主池、边池或平分赢家，范围内玩家的两张底牌同时公开，之后的玩家默认隐藏。
- 普通单赢家摊牌即使引擎派奖记录不带公共牌编号也必须正确公开；其他人全弃的赢家不强制亮牌；全下且下注结束时所有未弃牌者公开。
- 5 秒窗口内，本手玩家（含弃牌、刚离座者）可单击任一底牌公开该张，或“亮出全部”；其他玩家与观战者的牌桌、历史、日志不泄露未选中的牌。重复亮牌不重复写日志，亮牌不改变筹码。
- 5 秒窗口到期、错误手号、未参与本手或无效牌位的亮牌请求被拒绝。暂停不延长窗口，提前完成重买入不缩短窗口，关房等待窗口结束。
- 旧历史仅补齐原规则要求公开的赢家底牌，未公开的旧输家和弃牌底牌继续隐藏；重复加载不重复迁移。
- 每次行动先消耗基本 20 秒，之后自动消耗个人 TimeBank，默认容量 10 秒；全部耗尽后能过牌则过牌，否则弃牌。
- 默认每完成 20 手本人获发底牌的手牌补满 TimeBank；弃牌计数，观战与 AWAY 不计数；刷新、换设备和重新入座不能补满。
- 离线前 5 分钟保留座位并正常发牌、扣盲注和消耗行动时间；满 5 分钟停止参与新一手，恢复在线后需手动返回。主动 AWAY 不强制弃掉当前手牌。

## 重买入与筹码

- 不设置最低和最高买入选项，允许有效的正整数筹码买入，仍需审批。
- 玩家全下后暂时为零不触发重买入等待；整手结算后仍为零才触发，多人共用一个至多 20 秒的等待期。
- 例如两人归零：一人重买入到账、另一人离座后结束重买入等待；下一手仍须等满 5 秒亮牌窗口。若房主暂停或不足两位可参与玩家，仍不发下一手。
- 倒计时结束时未到账者自动离座，包括已提交但尚未获批者；迟到审批不能把筹码记入已释放座位。
- 重买入及补码不重复记账，不允许中途增加当前手可用筹码；重买入到账不覆盖主动 AWAY 或离线超时状态。
- 买出必须离座且全额结算，玩家和房主均无法部分买出；当前手参与者的离座在该手结算后执行。
- 核对累计买入、累计买出、当前筹码及净输赢；下注期间不将投入底池的筹码误展示为已实现亏损。

## 牌桌行动提示

- 过牌、下注、跟注、加注、全下和弃牌统一显示在玩家框外下注位置；本人、离线及 AWAY 玩家也能显示其本手行动，信息栏不重复展示行动名称。
- 跟注显示本轮累计下注额，例如小盲 1 跟注 1 后显示“跟注 2”；正常下注、加注及全下与引擎当前下注额一致，弃牌仅显示文字。
- 过牌后再次轮到该玩家时清除旧提示，发下一轮公共牌及本手结束也清除；超时过牌和刷新重连遵循同一规则。
- 桌面金额默认 18px，手机默认 16px，加粗且高对比度；验证九人桌、全部行动类型及大额数字，包含 1440、1024、390、360、320px 宽度，无横向溢出，无行动提示遮挡座位内容、底牌、公共牌或其他提示。
- 电脑端提示采用“行动 数额”单行，与所属玩家框边缘重叠 8px；左上两席右下对齐、右上两席左下对齐、左中右下对齐、右中左下对齐、左下右上对齐、右下左上对齐、本人上边居中。仅允许与所属玩家框边缘重叠，不能遮住昵称、筹码或底牌；手机保持现有布局。
- 电脑端底牌与玩家信息横向排列，公共牌放大，日志、统计和管理通过抽屉访问；验证 1366×768 笔记本及 800、761px 窄桌面窗口，本人底牌和牌型提示不被底部操作栏遮挡，亮牌与审批仍可正常操作。

## 恢复与管理

- 在下注、全下、多次发牌询问和结算边界模拟服务重启，原牌序和已确认行动可恢复；结算、买入和买出不重复。
- 重启后房间暂停，房主恢复时基本行动时间为 20 秒，TimeBank 保留已保存余额；多次发牌已提交选择保留，未回答者获得新的 15 秒。
- 房主离线不自动转让权限，牌局继续，审批等待；主动转让后原房主失去管理权。
- 普通暂停不打断当前手；结束房间打完当前手再全额买出，恢复暂停中请求结束也遵守此规则。
- 连续 24 小时无人在线关闭房间，关闭后分享链接可查公开记录，保留 30 天。

## 界面与交付

### 手机牌桌布局增量目标（第一轮访谈记录）

- 手机端保留现有配色和字体风格，底牌位于玩家信息框上方并部分叠在上沿，不遮住昵称及筹码；本人底牌更大，对手公开底牌可直接辨认点数与花色。
- 在 360×740、390×844 竖屏验证九人牌桌和行动操作可在一屏内访问；320×568 允许纵向滚动，但牌与按钮须可读、无横向溢出。特殊状态验收在后续确认后补充。
- 手机普通下注阶段加注金额、滑杆和行动按钮无需额外点击展开；未轮到本人时禁用，操作区高度稳定。Straddle、发两次牌投票、重买入及结算亮牌阶段在原位置显示对应操作。
- 两人、多人及九人满桌使用同一套九座相对布局；本人始终位于底部，其他玩家按实际座序排列。加入、离开时其他玩家不随人数重新排布，空位可选择并申请入座。

### 手机布局与双端摊牌呈现（第二轮访谈记录）

- 手机顶部房间信息压缩，底栏不重复展示本人昵称及筹码；本人玩家框可见 TimeBank，点击后可访问补码、AWAY、离座，房主可从醒目入口访问待审批申请。
- 手机行动提示贴近所属玩家：左右向桌心、顶部在框下、本人在底牌上；行动和完整金额单行显示，不遮住底牌、昵称或筹码。
- 手机和电脑均验证普通单组公共牌、发两次牌、平分及多底池结算；多组公共牌上下排列且同时可见，桌心显示当前底池，赢家在对应玩家框高亮，每位摊牌玩家的牌型在各自框内显示，移除结果详情。复杂赢家标注、获胜五张牌高亮及历史范围的验收在后续确认后补充。
- 本节及后续确认覆盖前文关于桌心结果区、结果选择与五张牌详情的冲突描述；既有自动摊牌和自愿亮牌的隐私规则继续适用。

### 双端摊牌呈现（第三轮访谈记录）

- 手机和电脑验证主池、边池由不同玩家获胜及平分底池时，所有赢家框同时高亮；发两次牌时各玩家框按第一次、第二次分行展示牌型，在对应行标注获胜，并在框内显示获胜数额。金额归并方式在后续确认后补充。
- 保留获胜五张牌的牌面高亮，包括公共牌和适用底牌；仅使用公共牌时不高亮底牌。移除原结果区后的高亮切换交互在后续确认后补充。
- 实时牌桌不显示结果区或详情入口；从既有手牌历史仍可核对各底池分配、赢家及五张牌。

### 复制与弹窗提示

- 邀请链接和召回码优先使用浏览器剪贴板接口；接口不可用或被拒绝时尝试兼容复制，仅在复制成功后提示“已复制”。召回码自动复制保持无分隔符格式。
- 自动复制均失败时选中对应输入框的全部文字，显示“请长按或手动复制”，保持弹窗打开。
- 弹窗打开期间，提示显示在当前弹窗内部，不被背景遮罩模糊，关闭提示不关闭弹窗；没有弹窗时使用页面提示。
- 桌面及手机窄屏验证成功、失败提示可见、可关闭且不遮挡输入或按钮；重复触发同一提示重新计时。

### 公共牌呈现与牌型提示

- 翻牌三张从左到右依次出现，间隔约 250 毫秒；转牌、河牌使用相同效果，全下和发两次牌遵守已确认的公共牌顺序，已有公共牌不重复发出。
- 公共牌发完后才启动下一位玩家的行动倒计时；全下发牌完成后展示赢家，并保留完整 5 秒亮牌窗口。刷新或重连不重播已发牌的动画或音效。
- 每张新增公共牌播放一次发牌音效；静音设置独立于其他参与者且能保留，浏览器禁止播放音效不影响牌局呈现。
- 摊牌结果区分公共牌组和主池、边池，所有平分赢家均列出牌型与五张牌，切换结果可核对对应高亮。无需摊牌获胜不额外公开赢家底牌或牌型。
- 本人从翻牌前即可看到当前成牌；弃牌后仍可见。牌型计算不提前使用尚未呈现的公共牌，也不向其他玩家或观战者透露本人的私有牌型。
- 使用“一对[A]”“两对[A,Q]”“三条[A]”“顺子”“同花”“葫芦”“四条[A]”“同花顺”“皇家同花顺”的显示格式；仅凭公共牌组成最佳牌型时也正确识别。
- 高牌使用“高牌[A]”；弃牌后的牌型继续更新，发两次牌时分别显示两组牌型。
- 同一公共牌组、同一底池的平分赢家同时高亮，每位赢家的五张牌可独立核对；仅使用公共牌时不高亮底牌，多组等价最佳五张固定选择。
- 历史记录可查看每个底池的赢家、牌型及五张牌，无动画和音效，也不包含私有成牌提示。

### 既有验收目标

- 桌面及手机竖屏均验证 9 个座位、本人底部位置、操作区、计时、Straddle 选择、重买入审批及日志统计，无遮挡或横向溢出。
- CSV 导出与房间统计一致，公开日志不泄露私密字段。
- 使用 Docker Compose 启动完整服务，运行多客户端端到端流程，并验证同时运行 20 个房间的目标。

### 手机布局与双端结果：最终确认

- 此节覆盖前文尚待确认项及冲突的桌心结果、点选高亮规则。360×740、390×844 竖屏可同时访问九个座位和固定操作区；320×568 允许牌桌区域纵向滚动。检查单组及两组公共牌、常规行动、亮牌窗口、重买入和审批入口，无横向溢出或内容遮挡。
- 金额及滑杆在手机普通下注阶段始终显示，未轮到本人、断线或不能加注时相应禁用；特殊阶段使用同一区域显示合法操作。轮换当前行动人不移动牌桌或改变操作区高度。
- 手机和电脑同时高亮全部主池赢家框，以及每组公共牌上各主池赢家实际使用的获胜五张牌；平分全部高亮、仅用公共牌时不高亮底牌。仅赢边池的玩家框和牌面不高亮，但其牌型和数额可见。赢家分得零筹码的奇数拆分边界仍依据赢家身份高亮。
- 每位公开摊牌玩家在自己的框内显示牌型，发两次牌时按公共牌编号分行，获胜数额按该组所有底池合计，不减去本手投入；无需摊牌获胜仍在玩家框显示获胜数额，不强制亮牌。
- 自动公开的输家也显示牌型；只亮一张牌时不公开完整牌型，亮出第二张后可更新。本人、其他玩家、观战者及手牌历史均遵守原有公开边界。
- 桌心只保留底池、公共牌及必要牌局状态，不显示赢家列表、结果详情或切换控件；已有手牌历史继续提供各底池分配、赢家及五张牌。
- 使用新生成的桌面和手机截图检查牌、昵称、筹码、牌型、获胜金额和行动提示；验证点击本人玩家框可以补码、AWAY、离座，且房主待审批入口只计未审批申请。

### PokerNow 玩家框布局

- 每个已入座玩家框主体只有昵称与筹码两行：昵称行保留必要的房主、离线、AWAY、弃牌、离座等待、重买入及倒计时标记，筹码行左侧显示完整当前筹码；不显示内部身份标识、头像或座位号，不因状态变化增加第三行。
- 结算时筹码行右侧仅对获胜玩家显示绿色 `+数额`，数额等于该玩家从本手所有公共牌组和主池、边池分得的总和，不扣除投入；发两次牌也只显示一个合计数额。未获筹码玩家保持空白，无需摊牌获胜不公开隐藏底牌或牌型，进入下一手后所有获胜数额消失。
- 电脑端两张底牌位于框外左侧并压住框边约 8px，牌型位于两张牌下方；本人实时牌型、弃牌后继续更新的本人牌型及完整公开两张底牌的摊牌玩家牌型均使用这一位置。发两次牌显示两行牌型。
- 手机端每位玩家底牌位于框上方且下缘被框轻微遮挡，本人底牌更大；本人牌型位于框下方，其他公开玩家的牌型贴住框右上角。右侧座位的牌型标签向左展开，所有标签均保持可读且不越出视口。
- 行动提示显示行动名称与完整本轮下注额。电脑端按既有九座角落规则与所属框重叠 8px；手机顶部、底部、左侧、右侧玩家的提示分别位于朝桌心的下、上、右、左侧，直接贴边的位置与所属框约重叠 4px，底部相邻座位允许留出避让本人手牌的小间距。提示不遮挡昵称、筹码、获胜数额、底牌或牌型。
- 使用九人桌验证长昵称、大额筹码、全部行动、普通摊牌、平分、主边池由不同玩家获胜、发两次牌、无需摊牌及只亮一张牌。在 1440×960、1366×768、1024×800、800×800、761×800、390×844、360×740 和 320×568 检查布局；360×740 与 390×844 无需滚动即可看牌和操作，320×568 允许纵向滚动，所有尺寸均无横向溢出。
- 除底牌压住玩家框、手机牌型压住玩家框右上角和行动提示压住玩家框边缘这些明确关系外，DOM 坐标检查不得出现内容碰撞。最终查看本轮新命名的桌面、手机和窄屏截图，不以旧截图作为完成证据。

### 玩家框视觉优化：第一轮已确认

- 本节按需求第 73–77 项覆盖上述冲突的计时、牌型位置及行动文字和字号要求；剩余数额汇总与窄屏容纳边界待下一轮确认。
- 电脑和手机检查长昵称、房主、离线、局内状态与基本倒计时/TimeBank 的组合。计时占筹码行右侧固定区域，昵称不与计时共用一行，不因基本计时切换至 TimeBank 而突然被挤没；边缘进度条仍可辨认。
- 当前成牌与公开摊牌牌型只显示一份，均位于玩家框底部浅色信息带；双公共牌两行分别标 ①、②，不以框外浮层遮挡底牌、昵称、筹码或操作区。验证本人弃牌后更新、公开两张底牌、仅亮一张及无需摊牌获胜的既有可见性规则。
- 下注、跟注、加注和全下胶囊只显示筹码图标及本轮累计下注额；过牌显示“过牌”，弃牌显示低调的“弃牌”。全下另在玩家框保留明确文字状态，不能仅靠颜色辨认。行动变更和下注轮结束时沿用原有清除规则。
- 行动金额以电脑 14px、手机 12px 为评估起点；使用本轮新截图检查整体缩小后的胶囊及牌型信息带，继续覆盖此前九人桌、双公共牌、长昵称和大额数字的视口矩阵，增加昵称行内部可读空间检查。

### 玩家框视觉优化：最终确认

- 本节按需求第 78–80 项覆盖上一节冲突和待确认项。玩家框内不出现基本倒计时或 TimeBank 数字；当前行动玩家在框底部显示时间条，进入额外思考时间时沿用颜色区别，行动结束后时间条移除。切换计时阶段不改变昵称可用宽度。
- 筹码右侧显示绿色 `+X` 获胜胶囊，与当前筹码及框底部牌型均不重叠。普通结算及双公共牌均显示本手所有底池的合计分配额，不减去投入；无获胜数额时不显示。无需摊牌获胜不额外公开牌型。
- 360×740、390×844 的普通场景保留一屏访问；复杂结算和大额数字允许牌桌区域纵向滚动。保留完整金额和牌型，操作区可访问，无横向溢出；320×568 继续允许纵向滚动。检查滚动后本人牌型及获胜胶囊可完整进入可视区域。
- 按钮位标记逐一轮换到九个座位，均不遮挡昵称、状态、筹码、牌型、底牌或行动胶囊。电脑在底牌下方留出标记位置；手机按所属座位在底牌旁避让。

### 行动胶囊朝向桌心并分离

- 按需求第 81 项检查两端九个座位，行动胶囊位于所属玩家区域朝桌心的一侧，文字水平。与玩家框的间距电脑至少 8px、手机至少 6px；需要越过底牌时同时避让底牌，不再允许胶囊覆盖所属框或底牌的边缘。
- 在既有八种视口验证全部行动、完整九位数下注额和本人行动提示；检查胶囊彼此之间以及与昵称、筹码、牌型、底牌、公共牌、按钮位标记之间均无碰撞。使用本轮 `artifacts/inward-*.png` 截图核对朝向与间距。

- 电脑端按第 82 项检查四个斜向座位：左下和左上 1 的胶囊在框右边界之外，右下和右上 1 在框左边界之外；下方两席的胶囊在框上边界之外，上方两席的胶囊在框下边界之外，各方向均至少留约 8px。有底牌时继续避让，窄桌面仍不得遮挡公共牌、底池或其他提示；本轮截图使用 `artifacts/corner-*.png`。

- 第 83 项进一步覆盖右侧两席的定位断言：右下胶囊右下角与手牌区域左上角之间横、纵各约 8px；右上 1 胶囊右上角与手牌区域左下角之间横、纵各约 8px。没有可见底牌时按相同预留区域检查，不回退到玩家框边缘。本轮截图使用 `artifacts/card-corner-*.png`。

- 按第 84 项补充电脑端成对对齐检查：左下与右下、左上 1 与右上 1 的胶囊顶部坐标分别相差不超过 1px，普通及大额数字均适用。上方一对与手牌下沿的间距允许随玩家框高度增加，以同时保证水平对齐和避让。截图使用 `artifacts/aligned-corner-*.png`。

- 手机端按第 85 项逐一检查两侧六席：下方一对的胶囊底边位于框顶边上方 6px，上方一对的顶边位于框底边下方 6px，中间一对与框的水平中线对齐，误差不超过 1px；左右方向分别在框右侧/左侧。普通及超长金额均不使用独立的上下错位偏移。
- 按第 86 项检查完整九位数金额分行后仍在胶囊内部，两侧各自占用受限宽度；本人提示及全部手牌、公共牌、底池、按钮位标记无遮挡。按钮位检查同时覆盖公共牌。正常 360×740、390×844 一屏可访问，320×568 及超长金额可滚动。截图使用 `artifacts/mobile-frame-*.png`。

### 手机行动胶囊精简

- 按需求第 87 项检查 320、360、390、760px 手机视口：行动胶囊不显示筹码图标及其占位，短金额按内容收缩，底部相邻两席也不再固定为 46px；所有行动金额保持 12px，完整九位数可按千分组换行且不溢出胶囊。
- “过牌”“弃牌”及玩家框内的全下标记可读，沿用九席定位、6px 间距及大额场景滚动规则。761px 及更宽桌面继续显示筹码图标，既有对齐、完整金额与碰撞检查通过。
- 使用本轮新截图 `artifacts/mobile-compact-20260911-*.png` 检查普通金额及超长金额下的手机、窄屏和桌面展示。

### 桌面空位对称

- 按需求第 88 项，在 761、800、1024、1100、1101、1366、1440px 桌面宽度检查仅本人入座时其余八个空位：四组左右空位的中心中点与牌桌中线偏差不超过 1px，成对纵坐标相差不超过 1px。覆盖本人实际座号为 0 和非 0 的旋转布局。
- 检查 760px 及常用手机尺寸沿用原有布局；已入座玩家的底牌、信息框和行动胶囊继续通过既有展示检查。使用本轮新截图 `artifacts/empty-seat-fixed-20260911-*.png` 核对效果。

### 手机牌桌宽度与轮廓

- 按需求第 89–90 项，检查 320、360、390、400、412、430、440、760px 手机视口：牌桌区域宽度为视口与 400px 中的较小值，水平居中；外沿、桌布和内框均呈竖向椭圆。761px 及更宽桌面保持原有布局。
- 验证空位、九人满桌、普通行动、完整九位数下注、双公共牌结算及长昵称/大额筹码；保持文字和牌面尺寸，玩家、底牌、公共牌、行动提示和按钮位标记无遮挡，无横向溢出。复杂状态沿用纵向滚动，不扩大牌桌宽度。
- 查看本轮 `artifacts/mobile-oval-fixed-20260911-*.png` 新截图确认宽屏手机实际效果。
- 按需求第 91 项检查手机等待开局时 RIVER ROOM 标识和“等待房主开局”可见，普通牌局继续显示标识及对应状态；状态提示位于公共牌下方且不挤动牌面。覆盖 320–760px 的等待、九人行动、大额下注和双公共牌结算场景，检查两处文字与座位、底牌、公共牌、底池、边池、按钮位及行动提示均无碰撞。截图使用 `artifacts/mobile-labels-fixed-20260911-*.png`。

### 首次开局前底牌轮廓

- 按需求第 92 项检查首次开局前：每位已入座玩家恰好显示两张淡色磨砂占位牌，灰绿色半透明填充、前亮后暗、边缘柔和，重叠处后牌线条弱化。每张牌中央显示低透明度的 River Room 小黑桃品牌标记，后牌标记更淡，空座位无占位牌；辅助阅读提示为“底牌区域，尚未发牌”。
- 检查桌面及手机视口，轮廓沿用底牌尺寸和排布，不遮挡昵称或筹码，无横向溢出。开始游戏后轮廓消失，本人牌面和对手牌背正常；局内及局间不出现该轮廓。

### 弃牌底牌展示

- 按需求第 93–94 项检查本人弃牌后两张真实牌面仍可读，亮度约为正常的 55%；其他玩家与观战者看到两张磨砂牌，每张中央各一个 ×，手机和电脑均可见，尺寸、排布及占位保持稳定。第 92 项的开局前小黑桃磨砂牌不受影响。
- 覆盖弃牌后仍在进行的牌局、结算亮牌窗口、窗口结束后的暂停等待、刷新与下一手发牌：弃牌展示持续到新牌发出，新手恢复正常牌面/牌背；未弃牌但摊牌落败者不显示弃牌效果。本人成牌继续更新。
- 弃牌玩家亮一张时，本人两张牌维持暗色，其他视角显示一张正常亮度的真实牌加一张磨砂 ×；亮两张时，其他视角显示两张正常牌。核对公开牌数据与历史记录，未公开点数和花色不得泄漏。
- 使用本轮 `artifacts/folded-cards-20260911-*.png` 核对桌面、手机及窄屏效果，并检查牌面、昵称、筹码、行动提示无碰撞与横向溢出。本项覆盖前述局内、局间不出现磨砂轮廓的弃牌场景。


### 筹码、获胜牌与手机底牌（2026-09-17）

- 在 320、360、390、760、761、1440px 验证筹码与获胜胶囊：常规金额不被挤成两截，大额 `123,456,789` 与 `+987,654,321` 全量可见，各自保持单行，空间不足时胶囊整体换行，无横向溢出。
- 单赢家、平分、主池和边池赢家不同、双公共牌及仅用公共牌成牌场景中，突出主池获胜组合并集，各组公共牌独立判断，其余可见真实牌调暗至约 55%。隐藏牌保持隐藏，无需摊牌获胜不触发突出和调暗。
- 新结算只播放一次约 220ms 的上移动画，公共牌 8px、底牌 6px；最终位置保持至下一手替换或牌面清除。重连、刷新及亮牌窗口到期不重播。系统减少动态效果偏好下直接显示最终状态。
- 手机全部座位底牌交叠且右牌在前，本人牌更大；点数、花色和两张牌的独立亮牌点击区域均可用。牌面、牌背及两类占位牌排列一致；上移与旋转并存，不遮挡昵称或操作。
- 本人弃牌底牌与非成牌调暗不叠加滤镜；后续新手恢复正常牌面。原有牌型、金额口径、公开边界及玩家框赢家范围保持有效。

- 手机交叠可读性：在 320/360/390/760px 九座布局中检查双字符点数 10 和四种花色，字符不能被前牌或玩家框遮挡；除包围盒无越界外，按真实绘制层级检查字符左右侧及下缘采样点，继续验证两张牌可分别点击。

### 玩家成就徽章（2026-09-17 本地验收通过）

本节对应需求第 99–105 项及悬挂位置修订。生产构建通过，86 项后端测试、35 项浏览器测试通过；使用本地独立 SQLite 与 Chromium，未部署。实现、场景与截图记录见 `docs/player-achievement-badges.md`。

- 普通独赢主池、对手全部弃牌取得主池各加一次；仅赢边池、主池平分不加。独赢主池同时赢多个边池仍只加一次。
- 发两次牌独赢两组主池加一次，边池归其他人也计；仅赢一组、两组不同赢家、其中一组平分或两组均平分均不加。奇数筹码分配不能把并列赢家误判为独赢。
- 全下后赢回筹码不增加清台；本手结算归零加一次，排队补码立即到账或随后离座仍计。主动买出、未入座零筹码、全下尚未结算均不计。
- 同一参与者离座再入座、重连、召回及服务重启后数值保持；重复处理同一手、重复回填历史不重复累计。历史超过 100 手仍计入完整记录。
- 既有房间两个计数与完整历史逐手核对；历史缺少必要信息时展示真实统计起点，不伪装为完整累计，也不根据正收益或当前筹码猜测旧结果。
- 在 0、1、99、100 和大数时验证角标：零值隐藏对应徽章；超过 99 显示 `99+`；玩家信息展示完整数字与解释。只有一种非零时只显示一枚。
- 每枚图标和数字共用一个紧凑轮廓，金色奖杯在前、红色碎裂筹码在后；不拆成两个独立标签。点击玩家框继续打开现有玩家信息并可访问原有操作；更新不播放独立动画。
- 手机与电脑两枚徽章均独立并排，包围盒之间至少留 2px，图案和数字不互相覆盖；不以负间距压缩两枚徽章。
- 单枚徽章尺寸不超过电脑 19×18px、手机 14×14px；在 320/360/390/760px 检查手机全部座位的每张底牌，含旋转后的牌面边缘，左右不得超出所属玩家框。保留底牌尺寸和字号。
- 无徽章与两枚徽章状态之间，本人和其他玩家的底牌相对玩家框的纵向位置完全一致；徽章避让只向左移动，不新增底牌上抬。既有获胜牌动效继续独立验证。
- 徽章独立叠放在玩家框上层、跨过右上边缘，悬挂部分不被框裁切；同一状态下隐藏和显示徽章时，玩家框高度与内部排布不变。验证手机底牌避让后没有越出屏幕，点数、花色及独立亮牌操作仍可用。
- 在 320、360、390、760、761、1440px 验证九个座位，覆盖长昵称、房主、离线、全下、AWAY、双公共牌、大额筹码及获胜数额。徽章不遮挡底牌和独立亮牌点击区、昵称、状态、筹码、牌型、时间条或行动胶囊，无横向溢出。

### 九人实测修复（2026-09-18）

- 正常四人牌局的上一手大盲离座或 AWAY 后，出现空小盲；其他玩家全弃时，投入全部退回、历史增加一手、唯一未弃牌者赢池加一、无人清台，手数与 TimeBank 正常推进。新旧规则手均验证重启暂停、房主结束、超时推进、亮牌期限后关闭以及筹码守恒，不能重复计数或泄露底牌。
- 成就某指标失败时保存待补算手号，另一指标照常更新；后续手不清空已有计数，有限重试不会饿死后续缺口；恢复补算幂等。补算落库失败须连同计数和待补算状态一起回滚。旧成就版本迁移保留原数值与统计起点，前端只在对应指标详情提示待补算。
- 五人投入 1/1/2/2/2、主池 5 和边池 3，双公共牌分别由两位深码玩家独赢：新规则得到 5/3，原池明细保留 5、3 及原资格；未标版本的旧手仍为原有 4/4，操作重放与实际结算相同。
- 双发最后一组多人平分、余数使原先无获池者分到 1 时，所有筹码必须收回玩家筹码，不残留在下注区；实际分得 0 的并列赢家仍保留赢家身份，不误计独赢。
- Straddle 4 首加最低 8、下一次最低 12；已跟 4 遇到全下 6 不重开，连续短全下累计达到完整增量才重开；翻牌后最低下注 2。旧手加到 6 的操作仍可恢复。
- 暂停结算超过 5 秒、刷新、下一手 Straddle 询问时，获胜金额、牌型及赢家框持续；新增亮牌操作仍被禁止。新发牌时旧结果清除；旧玩家离座、新人占同席及本人换席不串用结果。
- 320px 复杂双组结算只有点击“查看我的结果”才滚动，本人牌型完整进入可视区域且不被操作区遮挡；桌面不显示此提示。沿用既有 320/360/390/760/761/1440px 九座、金额、牌面和徽章布局回归。
- 暂停状态文案准确；单组日志省略组号，双组日志/历史统一组名；历史展示分池资格、“本手净输赢”及“获池金额”，不混淆数值口径。

本节为本地修复验收；线上两个阻塞测试房的实际收尾需部署后另行核验。性能专项按用户要求排除。

### 2–7 杂色奖励（2026-09-18）

- 新建房默认关闭，信息按钮点击可查看完整规则且不会创建房间或切换开关；移动端可关闭规则弹窗并回到原设置。首次开启默认一个大盲，正整数校验生效，金额修改后切换开关、改盲注、Straddle 均不自动重置。
- 游戏进行中只能由房主修改奖励开关及金额，其他字段服务端拒绝修改；当前手仍使用准备发牌时的规则。所有人可见当前及下一手变更，Straddle 询问期间修改留到随后一手；准备被取消后重新取最新规则。
- 杂色 7/2 和 2/7 均匹配，同花及其他底牌不匹配。覆盖翻牌前全弃、正常摊牌、独赢主池而输边池、仅赢边池、主池平分、双发全赢/只赢一组/任一组平分、无底池投入退回。每手至多奖励一次。
- 本手名单以外不付款，已弃牌、断线、待离座者仍支付。逐人验证足额、部分余额及零余额，扣款不超过底池结算后余额，余额非负、筹码守恒。补码在奖励后到账，买出在奖励后执行。
- 奖励扣到零进入重买入等待并计清台一次；立即补码、离座以及成就重算均不漏记、不重复计数。获池金额不含奖励，本手净输赢及房间净输赢含实际奖励。
- 结算前私牌不泄露，符合规则后自动公开赢家两张，其他未亮牌者保持私密；实际奖励为零也自动亮牌、保留零实付记录。
- 正奖励与零奖励播放相同中央 Badge，金额分别为实际 +X 和 +0。验证约三秒自动消失、不阻断操作、不延长亮牌期或下一手发牌、已有对话框在上层；新手实际发牌时立即收起。
- 在线玩家及观战者看到新事件；持续状态推送不重复播放，首次进入、刷新、断线重连不补播旧事件。系统减少动态效果时静态展示且按时消失。
- 320/360/390/760/761/1440px 检查中央位置、完整长昵称与最大安全整数金额、规则入口及大额配置，页面无横向溢出。查看新的本地公屏和规则弹窗截图。
- 历史保留赢家、约定每人奖励、各人实付及合计；旧房迁移默认关闭、旧进行中手及旧 Straddle 准备不补发，重启保留当前手已固定规则，存储失败及重试不会出现半笔扣款或重复奖励。


### 鱿鱼游戏与手机徽章（2026-09-18）

- 创建、局中配置只允许房主操作；默认关闭、首次单价跟随当前大盲、后续保存金额，正整数与安全上限校验；规则按钮不误提交表单，嵌套弹窗退出正确。
- 两人一枚、多人 N−1 枚、每人每轮最多一枚；已有标记者再次独赢主池不增加标记，也不再次触发获鱿鱼自动亮牌。仅剩一名无标记者时结算，下一轮归零。新人首次获发牌才新增，准备取消不重复加入，AWAY/补码/重连/同身份重新入座不重复新增（2026-09-20 修订）。
- 获标记覆盖全弃、普通摊牌、主池边池不同赢家、平分含整数零份额、双发两组不同赢家、空底池。只认第一组主池；自动亮牌开/关分别验证，不在结算前公开私牌。
- 最后一名无标记者向其余每人支付一份单价，分别验证足额、短余额及零余额；不足额均分给持有者，整数尾差按本轮固定参与顺序分配，不因换座改变优先顺序。总筹码和净输赢守恒。
- 旧存档未完成轮次中，每人已有的多枚标记合并为一枚，保留参与名单、标记总量和筹码余额；已结算历史保持原样，恢复后继续至仅剩一名无标记者再结算。
- 先 2–7 再鱿鱼，鱿鱼后再清台、买出及补码；轮末记录含离座和 AWAY 玩家，正余额扣光计一次，早已零余额不反复计；成就回填使用同一完整记录。
- 离座/踢出/自动离座都保留责任及标记，余额暂留不提前买出；轮末或取消后只买出一次，CSV、统计含暂留余额；零额追加恢复入座不增加累计买入，仍需审批。
- 旧标记随下一手改价，当前手不变；Straddle 准备锁定，取消准备用新规则。先关后开以生效前最终值为准；关闭恰逢轮末先完成旧结算，否则取消。本轮已结算后关闭不撤销支付。
- 暂停、人数不足、重启保留轮次；无人可发牌时关闭仍可释放离座资金；主动及 24 小时自动结束房间均覆盖在座与离座余额。恢复重放、重复命令、保存失败重试不重复支付或记录。
- 桌面及手机显示规则、当前/下一手变化、已发/总数、全部轮次参与者、实付/应付、具体转账与取消原因；新事件短提示，刷新和重连不补播；观察者可见公开轮次状态。
- 本节替代旧手机徽章避让验收：0/1/2/3 枚徽章出现与消失时，手牌横纵坐标均不变；单枚 12×12px、间距 2px、全部徽章横排同高、紫色鱿鱼徽章显示本轮数量，成就保持累计语义。允许覆盖非信息区域，检查牌点、花色、弃牌 ×、昵称、状态、筹码和亮牌操作仍完整可用。
- 在 320/360/390/760/761/1440px 覆盖九席、本人及其他玩家、长昵称、99+、零枚鱿鱼、双公共牌、大额结果、弃牌与亮牌；不横向溢出，查看真实渲染截图。
- 本地完整后端测试、前端生产构建、独立 SQLite/Uvicorn 浏览器回归。通过本地验证不表示已部署或完成公网验收。


### 手机徽章横排与底牌角标修订（2026-09-18）

- 1/2/3 枚徽章保持横排、同高，手牌不随徽章数量偏移。底牌点数在左上，花色紧随其下，右下空白可被徽章覆盖；本人底牌保留较大字号，公共牌与桌面布局不变。
- 在 320/360/390/760px 九席、双公共牌、长昵称、大数与 99+ 下验证点数、花色、弃牌符号、庄家按钮不被徽章遮盖；双位数 10 的两端与花色下沿通过实际命中测试检查，不仅验证容器不溢出。
- 底牌尺寸、轻微旋转与交叠、获胜上移及弃牌明暗保持；两张底牌可分别点击亮牌。构建和现有徽章/牌面专项回归通过，查看 `artifacts/badges-corner-index-tie-390.png` 等新截图。

### 底池派彩与顺序跑马（2026-09-18，本地验收通过）

- 对应需求 135–142。双端桌心显示筹码图标和完整金额，玩家框中不再出现获胜数额胶囊；正常单赢家直接飞行，多赢家先拆分再同时飞行，拆分总额等于实际底池派彩。
- 覆盖同人主池和边池、同人赢两组、多位平分、主边池不同赢家；按实际正数收款人数生成胶囊，不固定为两个。实际分得零的并列赢家仍保留赢家身份，零底池不播放飞行。
- 固定牌序验证第一组发完并显示结果 1.5 秒后才开始第二组；第二组期间及最终画面均不显示第一组牌型、高亮和牌面，不泄露第二组未来牌，不重复发共享前缀。第一组独有赢家仍在统一派彩时收到正确金额。
- 底池拆分、飞行、到账翻页分别约 250/650/400 毫秒；第二组结果停留 1.5 秒后启动。拍取中间帧与阶段断言，不能仅靠最终截图判断通过。
- 2–7 转账在底池后、鱿鱼转账在 2–7 后；付款人和同一付款人的收款人均从庄家左侧顺时针逐人播放，每笔约 180 毫秒且不额外等待翻页。旋转视角不改变身份顺序，排序不改变实际分摊和余数分配。
- 付款飞出时扣展示余额、到达时增加收款展示余额；覆盖不足额、零额、收到后又支付、连续收到多笔以及同次结算后补码和买出。动画终态与真实账目一致，不使用最终余额减去获池金额猜测起始余额。
- 下注、盲注、退回投入、补码、派彩及惩罚均翻页；验证 980 → 1,240、跨千分位减少、0 值和连续更新。金额完整可读，无旧动画回写覆盖新值。
- 全部转账及其数字变化完成后才开始完整 5 秒亮牌和最多 20 秒重买入等待，两者并行；暂停、不足人数及 Straddle 不提前清除结果，下一手不得截断前手动画。
- 首次进入、刷新、重连、后台切回、系统减少动态效果、服务重启和重复快照不补播过期动效、不重复结算；对齐当前阶段且不泄露未发牌。窗口大小变化、滚动和座位变化不使飞行目标串到其他身份。
- 仅转账一方离座时，屏幕中央一次只显示一位离座者的昵称和当前剩余筹码，完成动画后再显示下一位。收付前余额、实付变化与最终余额一致，后台已买出不能让中央提前显示零；原座位已有新人时不误指向新人，不重复或漏播实际收付。
- 双方都离座时，中央同时显示付款人、收款人及各自当前剩余筹码：电脑左右排列、手机上下排列。筹码从付款方飞向收款方，飞出扣款、到达加款；不交替隐藏双方，不显示无关离座玩家，当前动画结束后才切换下一笔相关玩家。覆盖多个付款人及同一付款人多个收款人，金额、顺序和身份均正确。
- 320/360/390/760/761/1440px 覆盖九席、长昵称、完整大额、单组和双发；既有奖励公屏与鱿鱼提示不提前剧透或遮挡转账金额。中央离座玩家展示与牌面、牌型及模态框不冲突。

生产构建、160 项后端测试及 55 项浏览器测试通过；浏览器使用本地独立 SQLite/Uvicorn 与 Chromium，不代表线上验收。实现与截图见 `docs/ui-pot-settlement-2026-09-18.md`，尚未部署。

### 牌面比例与跑马阅读停留（2026-09-19）

- 对应需求 143–145。在 320×568、340×740、360×740、380×844、390×844、760×900、761×800、1024×800、1100×850、1101×850、1366×768、1440×850、1440×851、1440×960、1920×960、2048×1024 检查本人/他人底牌、牌背和公共牌/占位牌的实际 CSS 外框尺寸，宽高比统一 5:7，允许不足 0.001 的像素舍入误差。旋转后的包围盒不作为牌面比例。
- 单次和双次跑马覆盖翻牌前、翻牌后、转牌后进入全下：每个新发翻牌/转牌街段的动画完成后保留完整 1.5 秒，截止前不推进下一街、不提前派彩。河牌不额外叠加阅读停留，沿用既有结果展示；两组共享前缀不重复发出。
- 停留期间保留当前街独赢概率；持久化后读取仍保持同一截止时间，浏览器刷新后显示当前牌面与概率，不补播已结束动画。普通下注阶段翻牌仍按既有 750 毫秒完成并启动行动倒计时。
- 运行后端回归、前端生产构建及本地浏览器回归；检查新截图 `artifacts/cards-ratio-1366.png`、`artifacts/cards-ratio-1920.png`、`artifacts/cards-ratio-390.png`、`artifacts/cards-ratio-320.png`。本地通过不代表公网部署或验收。

本地验证结果：185 项后端测试及生产构建通过。64 项浏览器回归中 63 项首轮通过；另 1 项旧断言直接解析牌高变量，在变量改为比例公式后得到 NaN，修订为读取实际牌区下边界后单项复验通过，未放宽间距或碰撞要求。另在 320/390/1440px 补查大厅示例牌、弃牌明牌、亮牌选择器、历史底牌和获胜五张牌，比例均通过。使用独立 SQLite/Uvicorn 和 Chromium；本轮未部署。

# 验证记录

日期：2026-09-09。以下为本机验证，不代表公网部署或长期容量保证。

## PokerNow 玩家框布局（2026-09-10）

- `npm run build` 通过；`.venv/bin/python -m pytest backend/tests -q`：71 项通过；`BASE_URL=http://127.0.0.1:8005 npx playwright test`：25 项通过，最终运行约 26.8 秒。`git diff --check` 通过。
- 玩家框 DOM 只保留昵称行和筹码行；昵称行容纳房主、离线、局内状态、倒计时及本人 TimeBank，筹码行右侧在结算时显示整手合计获胜数额。浏览器断言确认牌桌内不存在头像和第三信息行，发两次牌只生成一个合计数额。
- 桌面逐座检查底牌位于框外左侧且与框横向重叠约 8px，牌型位于底牌下方；行动提示沿既有九座角落映射与所属框重叠 8px。手机逐座检查底牌下缘由框遮挡约 4px，本人牌型位于框下方，其他公开牌型与框右上角重叠且不越出视口。
- 检查 1440×960、1366×768、1024×800、800×800、761×800、390×844、360×740、320×568。覆盖九人行动、超大下注额、普通摊牌、九人平分、主边池不同赢家、发两次牌、本人弃牌后牌型更新、单张亮牌隐私、个人工具与审批入口；所有尺寸无横向溢出，360×740 与 390×844 无需滚动，320×568 保留允许的纵向滚动。
- 本轮独立截图为 `artifacts/player-frame-final-desktop-showdown-20260910-222711.png`、`player-frame-final-desktop-actions-20260910-222711.png`、`player-frame-final-mobile-showdown-20260910-222711.png`、`player-frame-final-mobile-actions-20260910-222711.png`，均在最终生产构建后重新生成并人工查看。
- 本地预览为 `http://127.0.0.1:8005`，独立 SQLite 数据库为 `data/player-frame-preview.db`；本轮未部署公网，手机证据来自 Chromium 视口模拟，未使用实体手机验证。

## 手机布局与双端摊牌呈现（2026-09-10）

- `npm run build` 通过；`.venv/bin/python -m pytest backend/tests -q`：71 项通过；`BASE_URL=http://127.0.0.1:8004 npx playwright test`：25 项通过，最终运行约 27.1 秒。`git diff --check` 通过。
- 手机改为纵向宽牌桌、底牌在玩家框上方、固定九座及本人底部位置；普通下注阶段加注金额、滑杆及行动按钮常驻，未轮到本人时禁用。个人工具可通过本人玩家框访问，待审批入口排除已批准的延后到账申请。
- 手机和电脑同时高亮所有主池赢家框及对应获胜五张牌，两组公共牌同时可见。仅赢边池的玩家显示牌型和获胜数额，不高亮；分得零筹码的主池平分赢家仍高亮。实时桌心结果区已移除，历史详情保留。
- 后端新增测试验证公开摊牌输家牌型、隐藏底牌边界、单张自愿亮牌不泄露牌型、亮出第二张后更新及历史同步；原有亮牌、计时、补码、筹码守恒、身份恢复和公共牌动画测试全部通过。
- 浏览器检查 1440×960、1366×768、1024×800、800×800、761×800、390×844、360×740、320×568。九人行动场景验证完整大额数字和提示碰撞；九人双公共牌场景检查 18 行牌型及数额、10 张公共牌同时高亮、筹码文字不被垂直裁切。360×740、390×844 检查牌桌不需滚动且座位不被操作区遮挡；320×568 允许牌桌区域滚动。
- 已人工查看新生成的 `artifacts/mobile-layout-table_actions-narrow.png`、`mobile-layout-nine_twice-narrow.png`、`mobile-layout-nine_twice-laptop.png`，并检查 `presentation-twice-mobile.png` 和桌面九人平分截图。其他对应视口截图位于 `artifacts/mobile-layout-*.png`。
- 本地预览为 `http://127.0.0.1:8004`，独立 SQLite 数据库为 `data/mobile-layout-preview.db`；`GET /api/health` 返回 `{"ok":true}`。未部署公网；本轮手机证据来自 Chromium 视口模拟，未使用实体手机验证。

## 复制邀请链接与弹窗提示修复（2026-09-10）

- `npm run build` 通过；`BASE_URL=http://127.0.0.1:8000 npx playwright test --max-failures=2`：21 项全部通过，包括新增 11 项复制与提示测试及原有房间、身份召回、牌局呈现和摊牌回归。
- 邀请链接和召回码分别验证原生 Clipboard API 成功、模拟 API 缺失、模拟权限拒绝、兼容复制返回失败及抛出异常。成功路径实际读取浏览器剪贴板核对内容，召回码保持无分隔符；失败路径验证输入聚焦、全文选中及提示可见。
- 验证关闭提示不会关闭弹窗、弹窗关闭后提示返回页面、切换弹窗后提示进入当前弹窗、服务端错误显示在弹窗内部，以及重复消息重置 4.5 秒计时。
- Chromium 检查 1440×960 和 320×568 视口，验证提示命中测试、边界和输入区不重叠；已查看 `artifacts/copy-native-1440.png`、`artifacts/copy-blocked-1440.png`、`artifacts/copy-blocked-320.png`。窄屏邀请按钮补充可访问名称。
- 本地预览：`http://127.0.0.1:8000`，使用独立 SQLite 数据库 `data/copy-preview.db`，健康检查通过。未部署公网；本轮未使用真实手机或内嵌浏览器验证。

## 桌面行动提示贴边验证

- 电脑端九个座位的行动提示按第 52 项定位，与所属玩家框重叠 8px，行动和金额单行显示；增加玩家框上下留白，避免压住底牌和文字。761–900px 窄窗口上移公共牌区域，给左右中席提示留出空间。
- `npm run build` 通过；`BASE_URL=http://localhost:8003 npx playwright test tests/presentation.spec.ts`：5 项通过。检查桌面九席同时显示提示、正常及大额数字、8px 重叠、单行排版、内容碰撞，以及公共牌发牌、摊牌和牌型展示回归。
- 检查 1440×960、1366×768、1024×800、800×800、761×800 桌面尺寸；390×844、360×740、320×568 沿用原有手机行动场景。已查看桌面、临界桌面宽度和手机截图，产物为 `artifacts/table-actions-*.png`。
- 扩展检查时发现原有手机布局在本人和相邻玩家同时显示行动提示的场景中存在碰撞，此问题不由本次桌面样式引入，留待手机布局调整；本轮手机回归维持原有场景范围。
- 本地现有预览服务 `http://localhost:8003` 已加载最新构建，未部署到公网。

## 牌桌行动与电脑布局验证

- `.venv/bin/python -m pytest backend/tests -q`：69 项通过。新增验证本轮累计下注额、下注与加注区分、主动及跟注全下、正常和超时过牌、再次轮到行动时清除、换轮及结算清除，以及恢复后不重现旧提示。
- `npm run build`：TypeScript 检查与生产构建通过。
- `BASE_URL=http://127.0.0.1:8003 npx playwright test`：完整 10 项通过，最终运行约 20.5 秒。覆盖全部牌桌行动、大额数字、桌面日志抽屉开关、入座审批、亮牌、重连及现有公共牌展示。
- 电脑端参照用户截图将底牌与玩家信息横向组合、放大公共牌并使用日志抽屉；九人桌在 1440×960、1366×768、1024×800、800×800、761×800、390×844、360×740、320×568 下验证。检查行动与座位、底牌、公共牌及其他提示的碰撞和文字溢出；桌面本人的底牌、牌型提示不被操作栏盖住。
- 截图见 `artifacts/table-actions-desktop.png`、`table-actions-laptop.png`、`table-actions-desktop-boundary.png`、`table-actions-mobile.png`，各视口另有大额数字版本。已人工检查桌面、笔记本、临界宽度、手机及九人摊牌截图。
- 本地预览为 `http://localhost:8003`，使用独立 SQLite 数据库 `data/table-actions-preview.db`，健康检查通过；重买入审批逻辑保持原规则。本次未部署到公网。

## 公共牌与牌型展示验证

- `.venv/bin/python -m pytest backend/tests -q`：64 项通过。覆盖逐街发牌、发牌期间拒绝行动且不消耗 TimeBank、结算后完整 5 秒窗口、重启恢复、全部牌型文字、公共牌优先的等价最佳五张、平分与主边池、两次发牌共用翻牌、合并派奖记录展开，以及分配为零的并列赢家。
- `npm run build`：TypeScript 检查与生产构建通过。
- Playwright 新增四项浏览器验证：记录翻牌 DOM 变化为 1、2、3 张并检查间隔；实际 Web Audio 发声调用恰好三次；刷新不重播、静音选择持久化；九人平分同时高亮，历史中列出每人五张牌；两次发牌和边池切换；弃牌后本人牌型更新且观战者无私有提示。
- `BASE_URL=http://127.0.0.1:8002 npm run test:e2e`：完整 9 项通过，约 18.0 秒；原有多浏览器入座审批、下注、刷新、身份召回、自愿亮牌及普通摊牌流程一并回归通过。
- 检查 `presentation-tie-desktop.png`、`presentation-tie-mobile.png`、`presentation-tie-narrow.png`、`presentation-tie-compact.png` 和 `presentation-twice-mobile.png`。覆盖 1440×960、390×844、360×740、320×568 视口；较短手机屏幕通过牌桌区域滚动保留操作区，结果列表、本人成牌提示和座位之间无重叠。
- `BASE_URL=http://127.0.0.1:8002 .venv/bin/python scripts/load_rooms.py`：20 个房间、40 个 WebSocket 连接、20 手全部完成，筹码核对通过，约 8.17 秒。脚本通过 WebSocket 等待发牌推进，关房等待亮牌窗口结束。
- 新特性预览服务运行于 `http://localhost:8002`，使用独立 SQLite 数据库 `data/features-preview.db`。既有历史加载时补充公开获胜五张牌，不额外公开旧记录中的隐藏底牌；已通过迁移测试验证。

## 摊牌修正验证

- `.venv/bin/python -m pytest backend/tests -q`：42 项通过。新增固定牌序覆盖普通单赢家、河牌最后加注、河牌全过、两人桌、平分、全下边池及两次发牌；验证单张公开、弃牌者自愿亮牌、旧手号与截止时间、重买入提前完成、离座及关房、重启后单张状态和历史迁移。
- `npm run build`：TypeScript 检查与生产构建通过。
- `BASE_URL=http://127.0.0.1:8001 npm run test:e2e`：5 项通过，约 10.1 秒。真实多浏览器连接验证普通摊牌的完整公开范围、弃牌后离座者单张亮牌与“亮出全部”、观战者牌桌和历史同步，以及暂停时 5 秒到期仍关闭入口。
- 检查 `showdown-desktop.png`、`showdown-narrow.png`、`showdown-mobile.png` 和 `showdown-observer.png`，覆盖桌面、390px 与 360px 手机视口；离座后本手座位保留至窗口结束，亮牌操作和公共牌无遮挡。
- 本次测试服务为 `http://127.0.0.1:8001`，使用独立 SQLite 数据库 `data/showdown-dev.db`；未重启原 8000 端口服务。旧库的赢家亮牌补齐会在新版服务加载该库时自动执行，迁移持久化与重复加载已用测试数据库验证。

## 此前基础验收

- 20 个后端测试通过：短码全下与加注权、主边池、两次发牌全员同意与拒绝超时、固定牌序重放、奇数筹码逐人分配、TimeBank、断线 AWAY、重买入等待及自动离座、全额买出、身份召回、命令去重、权限、事务回滚和历史底牌隐私。
- 3 个 Playwright 测试通过：多浏览器创建加入与审批、真实下注、刷新和设备召回；手机入口；九人桌的桌面、390px 和 360px 手机布局及底牌与座位状态的重叠检查。
- React/TypeScript 生产构建通过；后端直接提供生产界面时，3 个 Playwright 测试也全部通过，最终运行约 2.9 秒。
- 使用实际 PostgreSQL 的并发脚本通过：20 个同时运行的房间、40 个 WebSocket 连接、每房间一手牌并关闭，全部筹码核对一致；该次运行约 0.87 秒。这是快速功能并发检查，不是长期压测。
- Compose 配置校验通过。

## 环境限制

本机 Docker Desktop 未能启动，未执行容器镜像构建及 Compose 全栈启动。已使用本机 PostgreSQL 17 运行后端和浏览器验收。正式公网部署尚未进行，部署服务器由用户提供。

## 核查产物

截图位于 `artifacts/`：入口页面、真实牌局、九人桌、摊牌和手机布局。业务验收目标位于 `acceptance.md`。未公开底牌、随机牌序与召回码不进入公共日志；截图中仅展示测试身份。

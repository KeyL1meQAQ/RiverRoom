# 成就统计失败不阻断已验证的牌局结算

九人实测中，合法零派奖触发成就计算异常，导致整手结算反复回滚。成就属于派生统计，决定在核心牌局结算验证成功的前提下，将成就失败记录为待补算，保留此前已核实的计数并明确说明统计未更新，以避免非核心统计缺陷持续阻断游戏。

这一选择增加缺口记录、幂等补算和状态展示的成本，但优于继续阻断牌局、静默漏记或重置既有成就。筹码守恒、核心结算有效性和持久化失败仍须阻止提交；两个指标分别记录失败并幂等补算，详情提示部分手牌待补算，后续成功结算或服务恢复时有限重试。已核实旧计数与统计起点保留；没有既有成就数据的更老记录沿用原有历史回填边界。

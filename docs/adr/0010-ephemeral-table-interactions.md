# 桌面互动使用不持久化的实时事件

气泡和投掷只供当前在线参与者短暂观看，不属于牌局事实或房间日志。现有业务命令会保存整份房间状态并广播完整视图，因此互动请求应单独验证身份、预设、目标与频率后，以独立 WebSocket 事件广播，不写入房间 JSON，也不在重连或服务重启后补播。当前部署配置为单个 Uvicorn 进程，进程内广播可以覆盖它持有的连接；将来增加多个进程或副本时，必须引入跨进程事件分发，不能继续依赖本地连接表。

# 使用房间内召回码接管匿名身份

产品要求无需注册或登录，同时允许参与者换浏览器或设备继续游戏。选择房间内独立的匿名身份，并以仅本人可见的召回码作为跨设备接管凭据；持码即可接管，无需原设备批准。接管时撤销旧设备操作权并轮换召回码，避免同一身份由多个设备同时行动。

这一选择让原设备不可用时仍可恢复身份，但召回码泄露也意味着他人可以接管，因此不能在公开日志或其他参与者界面展示召回码。不引入跨房间账号。本地身份及召回码同时丢失时，不允许凭昵称找回，也不允许房主代发召回码；房主可将旧身份离座结算，本人以新身份加入，旧账保留且不合并。
